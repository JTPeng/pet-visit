# P1 基础骨架 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搭建 NestJS 后端和 uni-app 前端的工程骨架，使所有后续业务模块（用户、宠物、社区等）可立即接入。

**Architecture:** 后端采用分层架构（Controller → Service → Repository via Prisma），全局注入统一响应/异常/日志/校验中间件，配置通过 ConfigModule 管理；前端采用 Pinia 状态管理、封装统一请求层（含 Token 注入与错误处理），通过 3 Tab 导航组织页面，所有请求走环境变量配置的 API 域名。

**Tech Stack:**
- 后端：NestJS 11 + TypeScript + Prisma + PostgreSQL + pino + class-validator
- 前端：uni-app + Vue 3 + TypeScript + Pinia + pinia-plugin-persistedstate
- 开发环境：Docker Compose（PostgreSQL）

**追踪**：每完成一步，勾选本文件与 [README.md](./README.md) 中对应条目。

---

## 项目文件结构目标（完成 P1 后的状态）

> **说明**：P1 只搭建 `serve/` 和 `wx-app/`。`admin/` 工程在 P7 阶段初始化，这里先在总览里列出，保证全局视图完整。

### 仓库根结构

```
pet-visit/
├── serve/           # NestJS 后端（P1 搭建基础骨架）
├── wx-app/          # uni-app 小程序前端（P1 搭建基础骨架）
├── admin/           # 移动端 H5 管理后台（P7 才创建，P1 不涉及）
└── docs/
```

### serve/（后端）

```
serve/
├── prisma/
│   └── schema.prisma                   # 数据模型（目前仅占位）
├── src/
│   ├── common/
│   │   ├── filters/
│   │   │   └── http-exception.filter.ts
│   │   ├── interceptors/
│   │   │   └── response.interceptor.ts
│   │   ├── dto/
│   │   │   └── api-response.dto.ts
│   │   └── constants/
│   │       └── error-code.ts
│   ├── config/
│   │   ├── config.module.ts
│   │   ├── configuration.ts            # 环境变量加载与类型化
│   │   └── env.validation.ts           # 环境变量校验
│   ├── modules/
│   │   └── health/
│   │       ├── health.controller.ts
│   │       └── health.module.ts
│   ├── prisma/
│   │   ├── prisma.module.ts            # 全局 Prisma 模块
│   │   └── prisma.service.ts
│   ├── logger/
│   │   └── logger.module.ts            # pino 封装
│   ├── app.module.ts
│   └── main.ts
├── .env.example
└── README.md
```

### wx-app/（前端）

```
wx-app/
├── src/
│   ├── api/
│   │   ├── request.ts                  # uni.request 封装
│   │   └── health.ts                   # 健康检查接口
│   ├── stores/
│   │   └── user.ts                     # 用户状态（P1 仅占位）
│   ├── utils/
│   │   └── env.ts                      # 环境配置读取
│   ├── types/
│   │   └── api.ts                      # API 响应通用类型
│   ├── components/
│   │   └── .gitkeep
│   ├── pages/
│   │   ├── index/index.vue             # Tab 1 首页
│   │   ├── publish/index.vue           # Tab 2 发帖
│   │   └── profile/index.vue           # Tab 3 我的
│   ├── static/
│   ├── App.vue
│   ├── main.ts
│   ├── manifest.json
│   ├── pages.json                      # 3 Tab 配置
│   └── uni.scss                        # 主题变量
└── .env.development / .env.production  # uni-app 环境变量
```

---

## 前置准备

- [x] **Step 0.1：确认工作目录**

Run: `pwd && ls /Users/timer/Document/Code/ai-code/pet-visit`
Expected: 能看到 `serve/`、`wx-app/`、`docs/`

---

## Task P1-T1：后端核心依赖安装

**Files:**
- Modify: `serve/package.json`

- [x] **Step 1：安装核心依赖**

Run:
```bash
cd /Users/timer/Document/Code/ai-code/pet-visit/serve && pnpm add @nestjs/config prisma @prisma/client class-validator class-transformer nestjs-pino pino pino-http pino-pretty
```

Expected: `package.json` 中 dependencies 新增对应包，无错误。

- [x] **Step 2：验证构建仍可通过**

Run:
```bash
cd /Users/timer/Document/Code/ai-code/pet-visit/serve && pnpm build
```

Expected: `dist/` 目录更新，无 TypeScript 错误。

- [x] **Step 3：提交**

```bash
git add serve/package.json serve/pnpm-lock.yaml
git commit -m "chore(serve): install core dependencies for P1 foundation"
```

---

## Task P1-T2：后端环境配置模块

**Files:**
- Create: `serve/.env.example`
- Create: `serve/src/config/configuration.ts`
- Create: `serve/src/config/env.validation.ts`
- Create: `serve/src/config/config.module.ts`
- Modify: `serve/src/app.module.ts`

- [x] **Step 1：创建 `.env.example`**

文件内容：
```env
NODE_ENV=development
PORT=8090

DATABASE_URL=postgresql://pet_visit@localhost:5432/pet_visit?schema=public

LOG_LEVEL=debug
```

说明：本地开发使用 Homebrew 安装的 PostgreSQL，默认对 localhost 连接采用 trust 策略，DATABASE_URL 中无需密码。生产环境务必配置密码并改为 `require` SSL。

- [x] **Step 2：创建 `serve/src/config/configuration.ts`**

```typescript
export interface AppConfig {
  nodeEnv: string;
  port: number;
  databaseUrl: string;
  logLevel: string;
}

export default (): AppConfig => ({
  nodeEnv: process.env.NODE_ENV ?? 'development',
  port: parseInt(process.env.PORT ?? '8090', 10),
  databaseUrl: process.env.DATABASE_URL ?? '',
  logLevel: process.env.LOG_LEVEL ?? 'info',
});
```

- [x] **Step 3：创建 `serve/src/config/env.validation.ts`**

```typescript
import { plainToInstance } from 'class-transformer';
import { IsIn, IsInt, IsString, Min, validateSync } from 'class-validator';

class EnvironmentVariables {
  @IsIn(['development', 'production', 'test'])
  NODE_ENV: string;

  @IsInt()
  @Min(1)
  PORT: number;

  @IsString()
  DATABASE_URL: string;

  @IsString()
  LOG_LEVEL: string;
}

export function validateEnv(config: Record<string, unknown>) {
  const validated = plainToInstance(EnvironmentVariables, config, {
    enableImplicitConversion: true,
  });
  const errors = validateSync(validated, { skipMissingProperties: false });
  if (errors.length > 0) {
    throw new Error(errors.toString());
  }
  return validated;
}
```

- [x] **Step 4：创建 `serve/src/config/config.module.ts`**

```typescript
import { Module } from '@nestjs/common';
import { ConfigModule as NestConfigModule } from '@nestjs/config';
import configuration from './configuration';
import { validateEnv } from './env.validation';

@Module({
  imports: [
    NestConfigModule.forRoot({
      isGlobal: true,
      load: [configuration],
      validate: validateEnv,
    }),
  ],
})
export class ConfigModule {}
```

- [x] **Step 5：在 `serve/src/app.module.ts` 引入**

```typescript
import { Module } from '@nestjs/common';
import { AppController } from './app.controller';
import { AppService } from './app.service';
import { ConfigModule } from './config/config.module';

@Module({
  imports: [ConfigModule],
  controllers: [AppController],
  providers: [AppService],
})
export class AppModule {}
```

- [x] **Step 6：创建本地 `.env` 并启动**

```bash
cd /Users/timer/Document/Code/ai-code/pet-visit/serve && cp .env.example .env && pnpm start
```

Expected: 启动成功，输出 `Nest application successfully started`。

按 Ctrl+C 停止。

- [x] **Step 7：提交**

```bash
git add serve/.env.example serve/src/config/ serve/src/app.module.ts
git commit -m "feat(serve): add ConfigModule with env validation"
```

---

## Task P1-T3：本地 PostgreSQL（Homebrew）

**Files:**（此任务不修改项目文件，只是本地环境准备）

> 需要一次性在本机安装 PostgreSQL 16。已安装可跳过对应步骤。

- [x] **Step 1：检查是否已安装 PostgreSQL**

Run:
```bash
brew list --versions postgresql@16 2>/dev/null || echo "not installed"
```

若输出 `not installed`，进入 Step 2；否则直接进入 Step 3。

- [x] **Step 2：安装 PostgreSQL 16**

Run:
```bash
brew install postgresql@16
```

Expected: 安装完成，末尾提示包含 `To start postgresql@16 now and restart at login` 等说明。

- [x] **Step 3：启动 PostgreSQL 服务**

Run:
```bash
brew services start postgresql@16
```

Expected: `Successfully started postgresql@16`。

- [x] **Step 4：把 postgresql@16 的 bin 目录临时加到当前 shell PATH**

Run:
```bash
export PATH="/opt/homebrew/opt/postgresql@16/bin:$PATH"
which psql
```

Expected: 输出 `/opt/homebrew/opt/postgresql@16/bin/psql`（Apple Silicon）或 `/usr/local/opt/postgresql@16/bin/psql`（Intel）。

说明：为避免污染 shell 配置文件，不写入 `~/.zshrc`。后续开发只需让后端能通过 `DATABASE_URL` 连接即可，不依赖 PATH。

- [x] **Step 5：创建数据库用户和数据库**

Run:
```bash
createuser -s pet_visit 2>/dev/null || echo "user pet_visit already exists"
createdb -O pet_visit pet_visit 2>/dev/null || echo "database pet_visit already exists"
```

Expected: 两条命令中至少一条静默成功，或打印 `already exists`。

- [x] **Step 6：验证连接**

Run:
```bash
psql -U pet_visit -d pet_visit -c "SELECT version();"
```

Expected: 返回 PostgreSQL 16.x 版本字符串。

- [x] **Step 7：（无需提交）**

本任务不修改项目文件，跳过 commit。

---

## Task P1-T4：后端 Prisma 接入

**Files:**
- Create: `serve/prisma/schema.prisma`
- Create: `serve/src/prisma/prisma.service.ts`
- Create: `serve/src/prisma/prisma.module.ts`
- Modify: `serve/src/app.module.ts`
- Modify: `serve/package.json`（新增 scripts）

- [x] **Step 1：初始化 Prisma**

```bash
cd /Users/timer/Document/Code/ai-code/pet-visit/serve && pnpm exec prisma init --datasource-provider postgresql
```

Expected: 生成 `prisma/schema.prisma` 和 `.env`（我们已有，合并或忽略）。

- [x] **Step 2：替换 `serve/prisma/schema.prisma` 内容为**

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// P1 阶段占位：后续模块会在此添加 model
model _HealthCheck {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
}
```

- [x] **Step 3：创建 `serve/src/prisma/prisma.service.ts`**

```typescript
import { Injectable, OnModuleDestroy, OnModuleInit } from '@nestjs/common';
import { PrismaClient } from '@prisma/client';

@Injectable()
export class PrismaService
  extends PrismaClient
  implements OnModuleInit, OnModuleDestroy
{
  async onModuleInit() {
    await this.$connect();
  }

  async onModuleDestroy() {
    await this.$disconnect();
  }
}
```

- [x] **Step 4：创建 `serve/src/prisma/prisma.module.ts`**

```typescript
import { Global, Module } from '@nestjs/common';
import { PrismaService } from './prisma.service';

@Global()
@Module({
  providers: [PrismaService],
  exports: [PrismaService],
})
export class PrismaModule {}
```

- [x] **Step 5：在 `serve/src/app.module.ts` 引入 PrismaModule**

```typescript
import { Module } from '@nestjs/common';
import { AppController } from './app.controller';
import { AppService } from './app.service';
import { ConfigModule } from './config/config.module';
import { PrismaModule } from './prisma/prisma.module';

@Module({
  imports: [ConfigModule, PrismaModule],
  controllers: [AppController],
  providers: [AppService],
})
export class AppModule {}
```

- [x] **Step 6：在 `serve/package.json` 的 scripts 中新增**

```json
"prisma:migrate": "prisma migrate dev",
"prisma:generate": "prisma generate",
"prisma:studio": "prisma studio"
```

- [x] **Step 7：执行首次 migration**

```bash
cd /Users/timer/Document/Code/ai-code/pet-visit/serve && pnpm prisma:migrate --name init
```

Expected: 生成 `prisma/migrations/xxx_init/`，数据库创建 `_HealthCheck` 表。

- [x] **Step 8：启动后端验证**

```bash
pnpm start
```

Expected: 启动成功，无数据库连接错误。Ctrl+C 停止。

- [x] **Step 9：提交**

```bash
git add serve/prisma serve/src/prisma serve/src/app.module.ts serve/package.json
git commit -m "feat(serve): integrate Prisma with PostgreSQL"
```

---

## Task P1-T5：后端统一响应拦截器

**Files:**
- Create: `serve/src/common/dto/api-response.dto.ts`
- Create: `serve/src/common/interceptors/response.interceptor.ts`
- Modify: `serve/src/main.ts`

- [x] **Step 1：创建 `serve/src/common/dto/api-response.dto.ts`**

```typescript
export interface ApiResponse<T = unknown> {
  code: number;
  message: string;
  data: T | null;
  timestamp: number;
}
```

- [x] **Step 2：创建 `serve/src/common/interceptors/response.interceptor.ts`**

```typescript
import {
  CallHandler,
  ExecutionContext,
  Injectable,
  NestInterceptor,
} from '@nestjs/common';
import { Observable } from 'rxjs';
import { map } from 'rxjs/operators';
import { ApiResponse } from '../dto/api-response.dto';

@Injectable()
export class ResponseInterceptor<T>
  implements NestInterceptor<T, ApiResponse<T>>
{
  intercept(
    _context: ExecutionContext,
    next: CallHandler,
  ): Observable<ApiResponse<T>> {
    return next.handle().pipe(
      map((data) => ({
        code: 0,
        message: 'success',
        data: data ?? null,
        timestamp: Date.now(),
      })),
    );
  }
}
```

- [x] **Step 3：在 `serve/src/main.ts` 注册**

```typescript
import { NestFactory } from '@nestjs/core';
import { AppModule } from './app.module';
import { ResponseInterceptor } from './common/interceptors/response.interceptor';

async function bootstrap() {
  const app = await NestFactory.create(AppModule);
  app.useGlobalInterceptors(new ResponseInterceptor());
  await app.listen(process.env.PORT ?? 8090);
}
bootstrap();
```

- [x] **Step 4：提交**

```bash
git add serve/src/common/dto serve/src/common/interceptors serve/src/main.ts
git commit -m "feat(serve): add global response interceptor"
```

---

## Task P1-T6：后端统一异常过滤器

**Files:**
- Create: `serve/src/common/constants/error-code.ts`
- Create: `serve/src/common/filters/http-exception.filter.ts`
- Modify: `serve/src/main.ts`

- [x] **Step 1：创建 `serve/src/common/constants/error-code.ts`**

```typescript
export const ErrorCode = {
  SUCCESS: 0,
  PARAM_INVALID: 40000,
  UNAUTHORIZED: 40100,
  FORBIDDEN: 40300,
  NOT_FOUND: 40400,
  RATE_LIMITED: 42900,
  INTERNAL_ERROR: 50000,
} as const;

export type ErrorCodeValue = (typeof ErrorCode)[keyof typeof ErrorCode];
```

- [x] **Step 2：创建 `serve/src/common/filters/http-exception.filter.ts`**

```typescript
import {
  ArgumentsHost,
  Catch,
  ExceptionFilter,
  HttpException,
  HttpStatus,
  Logger,
} from '@nestjs/common';
import type { Response } from 'express';
import { ApiResponse } from '../dto/api-response.dto';
import { ErrorCode } from '../constants/error-code';

@Catch()
export class AllExceptionsFilter implements ExceptionFilter {
  private readonly logger = new Logger(AllExceptionsFilter.name);

  catch(exception: unknown, host: ArgumentsHost): void {
    const ctx = host.switchToHttp();
    const response = ctx.getResponse<Response>();

    let status = HttpStatus.INTERNAL_SERVER_ERROR;
    let code: number = ErrorCode.INTERNAL_ERROR;
    let message = 'Internal server error';

    if (exception instanceof HttpException) {
      status = exception.getStatus();
      const res = exception.getResponse();
      if (typeof res === 'string') {
        message = res;
      } else if (typeof res === 'object' && res !== null) {
        const r = res as { message?: string | string[]; error?: string };
        message = Array.isArray(r.message)
          ? r.message.join('; ')
          : r.message ?? r.error ?? message;
      }
      code = this.mapStatusToCode(status);
    } else if (exception instanceof Error) {
      message = exception.message;
    }

    this.logger.error(
      `HTTP ${status} ${message}`,
      exception instanceof Error ? exception.stack : undefined,
    );

    const body: ApiResponse<null> = {
      code,
      message,
      data: null,
      timestamp: Date.now(),
    };
    response.status(status).json(body);
  }

  private mapStatusToCode(status: number): number {
    switch (status) {
      case HttpStatus.BAD_REQUEST:
        return ErrorCode.PARAM_INVALID;
      case HttpStatus.UNAUTHORIZED:
        return ErrorCode.UNAUTHORIZED;
      case HttpStatus.FORBIDDEN:
        return ErrorCode.FORBIDDEN;
      case HttpStatus.NOT_FOUND:
        return ErrorCode.NOT_FOUND;
      case HttpStatus.TOO_MANY_REQUESTS:
        return ErrorCode.RATE_LIMITED;
      default:
        return ErrorCode.INTERNAL_ERROR;
    }
  }
}
```

- [x] **Step 3：在 `serve/src/main.ts` 注册**

```typescript
import { NestFactory } from '@nestjs/core';
import { AppModule } from './app.module';
import { ResponseInterceptor } from './common/interceptors/response.interceptor';
import { AllExceptionsFilter } from './common/filters/http-exception.filter';

async function bootstrap() {
  const app = await NestFactory.create(AppModule);
  app.useGlobalInterceptors(new ResponseInterceptor());
  app.useGlobalFilters(new AllExceptionsFilter());
  await app.listen(process.env.PORT ?? 8090);
}
bootstrap();
```

- [x] **Step 4：提交**

```bash
git add serve/src/common/constants serve/src/common/filters serve/src/main.ts
git commit -m "feat(serve): add global exception filter"
```

---

## Task P1-T7：后端参数校验管道（ValidationPipe）

**Files:**
- Modify: `serve/src/main.ts`

- [x] **Step 1：全局注册 ValidationPipe**

更新 `serve/src/main.ts`：

```typescript
import { NestFactory } from '@nestjs/core';
import { ValidationPipe } from '@nestjs/common';
import { AppModule } from './app.module';
import { ResponseInterceptor } from './common/interceptors/response.interceptor';
import { AllExceptionsFilter } from './common/filters/http-exception.filter';

async function bootstrap() {
  const app = await NestFactory.create(AppModule);
  app.useGlobalPipes(
    new ValidationPipe({
      whitelist: true,
      forbidNonWhitelisted: true,
      transform: true,
      transformOptions: { enableImplicitConversion: true },
    }),
  );
  app.useGlobalInterceptors(new ResponseInterceptor());
  app.useGlobalFilters(new AllExceptionsFilter());
  await app.listen(process.env.PORT ?? 8090);
}
bootstrap();
```

- [x] **Step 2：提交**

```bash
git add serve/src/main.ts
git commit -m "feat(serve): enable global ValidationPipe"
```

---

## Task P1-T8：后端日志（pino）

**Files:**
- Create: `serve/src/logger/logger.module.ts`
- Modify: `serve/src/app.module.ts`
- Modify: `serve/src/main.ts`

- [x] **Step 1：创建 `serve/src/logger/logger.module.ts`**

```typescript
import { Module } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { LoggerModule as PinoLoggerModule } from 'nestjs-pino';

@Module({
  imports: [
    PinoLoggerModule.forRootAsync({
      inject: [ConfigService],
      useFactory: (config: ConfigService) => ({
        pinoHttp: {
          level: config.get<string>('logLevel'),
          transport:
            config.get<string>('nodeEnv') === 'development'
              ? {
                  target: 'pino-pretty',
                  options: { singleLine: true, colorize: true },
                }
              : undefined,
          serializers: {
            req: (req) => ({
              id: req.id,
              method: req.method,
              url: req.url,
            }),
          },
        },
      }),
    }),
  ],
  exports: [PinoLoggerModule],
})
export class LoggerModule {}
```

- [x] **Step 2：在 `serve/src/app.module.ts` 引入**

```typescript
import { Module } from '@nestjs/common';
import { AppController } from './app.controller';
import { AppService } from './app.service';
import { ConfigModule } from './config/config.module';
import { PrismaModule } from './prisma/prisma.module';
import { LoggerModule } from './logger/logger.module';

@Module({
  imports: [ConfigModule, LoggerModule, PrismaModule],
  controllers: [AppController],
  providers: [AppService],
})
export class AppModule {}
```

- [x] **Step 3：在 `serve/src/main.ts` 使用 Logger**

```typescript
import { NestFactory } from '@nestjs/core';
import { ValidationPipe } from '@nestjs/common';
import { Logger } from 'nestjs-pino';
import { AppModule } from './app.module';
import { ResponseInterceptor } from './common/interceptors/response.interceptor';
import { AllExceptionsFilter } from './common/filters/http-exception.filter';

async function bootstrap() {
  const app = await NestFactory.create(AppModule, { bufferLogs: true });
  app.useLogger(app.get(Logger));
  app.useGlobalPipes(
    new ValidationPipe({
      whitelist: true,
      forbidNonWhitelisted: true,
      transform: true,
      transformOptions: { enableImplicitConversion: true },
    }),
  );
  app.useGlobalInterceptors(new ResponseInterceptor());
  app.useGlobalFilters(new AllExceptionsFilter());
  await app.listen(process.env.PORT ?? 8090);
}
bootstrap();
```

- [x] **Step 4：启动验证日志格式**

```bash
cd /Users/timer/Document/Code/ai-code/pet-visit/serve && pnpm start:dev
```

Expected: 彩色格式化日志输出。Ctrl+C 停止。

- [x] **Step 5：提交**

```bash
git add serve/src/logger serve/src/app.module.ts serve/src/main.ts
git commit -m "feat(serve): integrate pino logger"
```

---

## Task P1-T9：后端 CORS

**Files:**
- Modify: `serve/src/main.ts`

- [x] **Step 1：启用 CORS**

在 `bootstrap` 函数中添加：

```typescript
app.enableCors({
  origin: true,
  credentials: true,
});
```

位置：在 `useGlobalPipes` 之前。

- [x] **Step 2：提交**

```bash
git add serve/src/main.ts
git commit -m "feat(serve): enable CORS"
```

---

## Task P1-T10：后端健康检查接口

**Files:**
- Create: `serve/src/modules/health/health.controller.ts`
- Create: `serve/src/modules/health/health.module.ts`
- Modify: `serve/src/app.module.ts`
- Remove: `serve/src/app.controller.ts`、`serve/src/app.service.ts`、`serve/src/app.controller.spec.ts`（清理脚手架默认 Hello 接口）

- [x] **Step 1：创建 `serve/src/modules/health/health.controller.ts`**

```typescript
import { Controller, Get } from '@nestjs/common';
import { PrismaService } from '../../prisma/prisma.service';

@Controller('health')
export class HealthController {
  constructor(private readonly prisma: PrismaService) {}

  @Get()
  async check() {
    await this.prisma.$queryRaw`SELECT 1`;
    return {
      status: 'ok',
      uptime: process.uptime(),
    };
  }
}
```

- [x] **Step 2：创建 `serve/src/modules/health/health.module.ts`**

```typescript
import { Module } from '@nestjs/common';
import { HealthController } from './health.controller';

@Module({
  controllers: [HealthController],
})
export class HealthModule {}
```

- [x] **Step 3：清理默认 Hello 文件**

```bash
cd /Users/timer/Document/Code/ai-code/pet-visit/serve/src && rm app.controller.ts app.service.ts app.controller.spec.ts
```

- [x] **Step 4：更新 `serve/src/app.module.ts`**

```typescript
import { Module } from '@nestjs/common';
import { ConfigModule } from './config/config.module';
import { PrismaModule } from './prisma/prisma.module';
import { LoggerModule } from './logger/logger.module';
import { HealthModule } from './modules/health/health.module';

@Module({
  imports: [ConfigModule, LoggerModule, PrismaModule, HealthModule],
})
export class AppModule {}
```

- [x] **Step 5：启动并验证**

```bash
cd /Users/timer/Document/Code/ai-code/pet-visit/serve && pnpm start:dev
```

另开终端：
```bash
curl http://localhost:8090/health
```

Expected 返回：
```json
{"code":0,"message":"success","data":{"status":"ok","uptime":X.XX},"timestamp":1234567890}
```

Ctrl+C 停止。

- [x] **Step 6：提交**

```bash
git add serve/src
git commit -m "feat(serve): add health check endpoint and remove boilerplate"
```

---

## Task P1-T11：后端 API 全局前缀

**Files:**
- Modify: `serve/src/main.ts`

- [x] **Step 1：设置全局前缀 `/api`**

在 `bootstrap()` 中添加（CORS 之后、pipes 之前均可）：

```typescript
app.setGlobalPrefix('api');
```

- [x] **Step 2：验证新路径**

```bash
pnpm start:dev
# 另一终端
curl http://localhost:8090/api/health
```

Expected: 返回 JSON（code:0 ...）。旧路径 `/health` 不存在（404）。

- [x] **Step 3：提交**

```bash
git add serve/src/main.ts
git commit -m "feat(serve): add global /api prefix"
```

---

## Task P1-T12：前端依赖安装

**Files:**
- Modify: `wx-app/package.json`

- [x] **Step 1：安装 Pinia 及持久化插件**

```bash
cd /Users/timer/Document/Code/ai-code/pet-visit/wx-app && pnpm add pinia pinia-plugin-persistedstate
```

- [x] **Step 2：提交**

```bash
git add wx-app/package.json wx-app/pnpm-lock.yaml
git commit -m "chore(wx-app): install pinia and persistedstate plugin"
```

---

## Task P1-T13：前端环境配置

**Files:**
- Create: `wx-app/.env.development`
- Create: `wx-app/.env.production`
- Create: `wx-app/src/utils/env.ts`

- [x] **Step 1：创建 `wx-app/.env.development`**

```
VITE_API_BASE_URL=http://localhost:8090/api
```

- [x] **Step 2：创建 `wx-app/.env.production`**

```
VITE_API_BASE_URL=https://TODO_REPLACE_WITH_PROD_DOMAIN/api
```

- [x] **Step 3：创建 `wx-app/src/utils/env.ts`**

```typescript
interface AppEnv {
  apiBaseUrl: string;
}

export const env: AppEnv = {
  apiBaseUrl: import.meta.env.VITE_API_BASE_URL as string,
};
```

- [x] **Step 4：提交**

```bash
git add wx-app/.env.development wx-app/.env.production wx-app/src/utils
git commit -m "feat(wx-app): add env configuration"
```

---

## Task P1-T14：前端请求封装

**Files:**
- Create: `wx-app/src/types/api.ts`
- Create: `wx-app/src/api/request.ts`

- [x] **Step 1：创建 `wx-app/src/types/api.ts`**

```typescript
export interface ApiResponse<T = unknown> {
  code: number;
  message: string;
  data: T | null;
  timestamp: number;
}

export interface RequestOptions {
  url: string;
  method?: 'GET' | 'POST' | 'PUT' | 'DELETE' | 'PATCH';
  data?: Record<string, unknown> | unknown[];
  header?: Record<string, string>;
  skipAuth?: boolean;
}
```

- [x] **Step 2：创建 `wx-app/src/api/request.ts`**

```typescript
import { env } from '../utils/env';
import type { ApiResponse, RequestOptions } from '../types/api';

const TOKEN_STORAGE_KEY = 'pet_visit_token';

export function getToken(): string {
  return uni.getStorageSync(TOKEN_STORAGE_KEY) || '';
}

export function setToken(token: string): void {
  uni.setStorageSync(TOKEN_STORAGE_KEY, token);
}

export function clearToken(): void {
  uni.removeStorageSync(TOKEN_STORAGE_KEY);
}

export async function request<T = unknown>(
  options: RequestOptions,
): Promise<T> {
  const token = options.skipAuth ? '' : getToken();
  const header: Record<string, string> = {
    'Content-Type': 'application/json',
    ...(options.header ?? {}),
  };
  if (token) {
    header.Authorization = `Bearer ${token}`;
  }

  return new Promise<T>((resolve, reject) => {
    uni.request({
      url: `${env.apiBaseUrl}${options.url}`,
      method: options.method ?? 'GET',
      data: options.data,
      header,
      success: (res) => {
        const body = res.data as ApiResponse<T>;
        if (res.statusCode >= 200 && res.statusCode < 300 && body.code === 0) {
          resolve(body.data as T);
          return;
        }
        if (res.statusCode === 401 || body.code === 40100) {
          clearToken();
        }
        uni.showToast({
          title: body.message || '请求失败',
          icon: 'none',
        });
        reject(body);
      },
      fail: (err) => {
        uni.showToast({ title: '网络异常，请稍后再试', icon: 'none' });
        reject(err);
      },
    });
  });
}
```

- [x] **Step 3：提交**

```bash
git add wx-app/src/types wx-app/src/api/request.ts
git commit -m "feat(wx-app): add request wrapper with token injection"
```

---

## Task P1-T15：前端健康检查 API

**Files:**
- Create: `wx-app/src/api/health.ts`

- [x] **Step 1：创建 `wx-app/src/api/health.ts`**

```typescript
import { request } from './request';

export interface HealthResponse {
  status: string;
  uptime: number;
}

export function checkHealth() {
  return request<HealthResponse>({
    url: '/health',
    method: 'GET',
    skipAuth: true,
  });
}
```

- [x] **Step 2：提交**

```bash
git add wx-app/src/api/health.ts
git commit -m "feat(wx-app): add health check api"
```

---

## Task P1-T16：前端 Pinia 接入

**Files:**
- Create: `wx-app/src/stores/index.ts`
- Create: `wx-app/src/stores/user.ts`
- Modify: `wx-app/src/main.ts`

- [x] **Step 1：创建 `wx-app/src/stores/index.ts`**

```typescript
import { createPinia } from 'pinia';
import piniaPluginPersistedstate from 'pinia-plugin-persistedstate';

const pinia = createPinia();
pinia.use(
  piniaPluginPersistedstate({
    storage: {
      getItem: (key) => uni.getStorageSync(key) || null,
      setItem: (key, value) => uni.setStorageSync(key, value),
    },
  }),
);

export default pinia;
```

- [x] **Step 2：创建 `wx-app/src/stores/user.ts`**

```typescript
import { defineStore } from 'pinia';

interface UserState {
  token: string;
  openid: string;
  nickname: string;
  avatar: string;
  phone: string;
}

export const useUserStore = defineStore('user', {
  state: (): UserState => ({
    token: '',
    openid: '',
    nickname: '',
    avatar: '',
    phone: '',
  }),
  getters: {
    isLogin: (state) => Boolean(state.token),
    hasPhone: (state) => Boolean(state.phone),
  },
  actions: {
    setToken(token: string) {
      this.token = token;
    },
    logout() {
      this.$reset();
    },
  },
  persist: true,
});
```

- [x] **Step 3：修改 `wx-app/src/main.ts`**

```typescript
import { createSSRApp } from 'vue';
import App from './App.vue';
import pinia from './stores';

export function createApp() {
  const app = createSSRApp(App);
  app.use(pinia);
  return {
    app,
  };
}
```

- [x] **Step 4：提交**

```bash
git add wx-app/src/stores wx-app/src/main.ts
git commit -m "feat(wx-app): integrate pinia with persistence"
```

---

## Task P1-T17：前端 3 Tab 导航配置

**Files:**
- Modify: `wx-app/src/pages.json`
- Create: `wx-app/src/pages/publish/index.vue`
- Create: `wx-app/src/pages/profile/index.vue`
- Modify: `wx-app/src/pages/index/index.vue`
- Add: `wx-app/src/static/tabbar/`（3×2 = 6 张图标，先占位）

- [x] **Step 1：确认脚手架现有页面**

```bash
ls wx-app/src/pages
```

Expected: 看到 `index/`。

- [x] **Step 2：创建发帖页 `wx-app/src/pages/publish/index.vue`**

```vue
<template>
  <view class="page">
    <text class="title">发帖</text>
    <text class="hint">P6 阶段实现完整发帖功能</text>
  </view>
</template>

<script setup lang="ts"></script>

<style scoped>
.page {
  padding: 32rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
}
.title {
  font-size: 36rpx;
  font-weight: bold;
  margin-top: 120rpx;
}
.hint {
  margin-top: 24rpx;
  color: #999;
}
</style>
```

- [x] **Step 3：创建个人中心页 `wx-app/src/pages/profile/index.vue`**

```vue
<template>
  <view class="page">
    <text class="title">我的</text>
    <text class="hint">P2 阶段实现登录与资料，P5 阶段挂载宠物档案</text>
  </view>
</template>

<script setup lang="ts"></script>

<style scoped>
.page {
  padding: 32rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
}
.title {
  font-size: 36rpx;
  font-weight: bold;
  margin-top: 120rpx;
}
.hint {
  margin-top: 24rpx;
  color: #999;
}
</style>
```

- [x] **Step 4：更新 `wx-app/src/pages/index/index.vue`** 为占位首页（后续 P6 替换为信息流）

```vue
<template>
  <view class="page">
    <text class="title">首页</text>
    <button class="btn" @tap="handleCheck">调用健康检查</button>
    <text v-if="result" class="result">{{ result }}</text>
  </view>
</template>

<script setup lang="ts">
import { ref } from 'vue';
import { checkHealth } from '../../api/health';

const result = ref('');

async function handleCheck() {
  try {
    const data = await checkHealth();
    result.value = `后端正常: uptime=${data.uptime.toFixed(2)}s`;
  } catch (err) {
    result.value = '后端连接失败';
    console.error(err);
  }
}
</script>

<style scoped>
.page {
  padding: 32rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
}
.title {
  font-size: 36rpx;
  font-weight: bold;
  margin-top: 120rpx;
}
.btn {
  margin-top: 48rpx;
  width: 80%;
}
.result {
  margin-top: 32rpx;
  color: #333;
}
</style>
```

- [x] **Step 5：更新 `wx-app/src/pages.json`**

```json
{
  "pages": [
    {
      "path": "pages/index/index",
      "style": {
        "navigationBarTitleText": "首页"
      }
    },
    {
      "path": "pages/publish/index",
      "style": {
        "navigationBarTitleText": "发帖"
      }
    },
    {
      "path": "pages/profile/index",
      "style": {
        "navigationBarTitleText": "我的"
      }
    }
  ],
  "tabBar": {
    "color": "#7A7E83",
    "selectedColor": "#FF8A47",
    "backgroundColor": "#FFFFFF",
    "borderStyle": "black",
    "list": [
      {
        "pagePath": "pages/index/index",
        "text": "首页"
      },
      {
        "pagePath": "pages/publish/index",
        "text": "发帖"
      },
      {
        "pagePath": "pages/profile/index",
        "text": "我的"
      }
    ]
  },
  "globalStyle": {
    "navigationBarTextStyle": "black",
    "navigationBarTitleText": "宠物上门",
    "navigationBarBackgroundColor": "#FFFFFF",
    "backgroundColor": "#F8F8F8"
  }
}
```

说明：tabBar 图标暂不配置（uni-app 允许无 `iconPath` 的纯文字 tabBar）。P5/P6 正式开发时再替换成带图标版本。

- [x] **Step 6：启动前端验证**

```bash
cd /Users/timer/Document/Code/ai-code/pet-visit/wx-app && pnpm dev:h5
```

Expected: 浏览器打开，能看到 3 Tab，可切换，首页点"调用健康检查"按钮返回 `后端正常: uptime=...`。

（前提：后端已启动 `pnpm start:dev`）

- [x] **Step 7：提交**

```bash
git add wx-app/src/pages wx-app/src/pages.json
git commit -m "feat(wx-app): add 3-tab navigation with placeholder pages"
```

---

## Task P1-T18：前端通用主题样式

**Files:**
- Modify: `wx-app/src/uni.scss`

- [x] **Step 1：替换 `wx-app/src/uni.scss`** 为

```scss
$primary-color: #FF8A47;
$primary-color-light: #FFB288;
$text-color: #333;
$text-color-secondary: #999;
$bg-color: #F8F8F8;
$border-color: #EEEEEE;

$font-size-sm: 24rpx;
$font-size-base: 28rpx;
$font-size-lg: 32rpx;
$font-size-xl: 36rpx;

$spacing-xs: 8rpx;
$spacing-sm: 16rpx;
$spacing-md: 24rpx;
$spacing-lg: 32rpx;
$spacing-xl: 48rpx;
```

- [x] **Step 2：提交**

```bash
git add wx-app/src/uni.scss
git commit -m "feat(wx-app): add theme variables"
```

---

## Task P1-T19：联调测试

**Files:**（无修改，仅验证）

- [x] **Step 1：启动后端**

```bash
cd /Users/timer/Document/Code/ai-code/pet-visit/serve && pnpm start:dev
```

（确保 `brew services list` 中 `postgresql@16` 为 `started`）

- [x] **Step 2：另一终端启动前端**

```bash
cd /Users/timer/Document/Code/ai-code/pet-visit/wx-app && pnpm dev:h5
```

- [x] **Step 3：在浏览器中验证**

- 访问 H5 页面
- 点击底部 3 个 Tab，均能正常切换
- 首页点击"调用健康检查"，显示后端 uptime
- 打开浏览器开发者工具 Network，确认请求路径为 `http://localhost:8090/api/health`，响应格式为：
  ```json
  {"code":0,"message":"success","data":{"status":"ok","uptime":...},"timestamp":...}
  ```

- [x] **Step 4：关闭服务**

两个终端分别 Ctrl+C。

---

## Task P1-T20：更新 README 文档

**Files:**
- Modify: `serve/README.md`
- Create: `wx-app/README.md`

- [x] **Step 1：覆盖 `serve/README.md`** 为

```markdown
# pet-visit 后端

NestJS + TypeScript + Prisma + PostgreSQL。

## 启动开发环境

1. 本地 PostgreSQL（首次）：

```bash
brew install postgresql@16
brew services start postgresql@16
createuser -s pet_visit
createdb -O pet_visit pet_visit
```

2. 复制环境变量：

```bash
cp .env.example .env
```

3. 安装依赖并迁移数据库：

```bash
pnpm install
pnpm prisma:migrate
```

4. 启动：

```bash
pnpm start:dev
```

服务监听 `http://localhost:8090`，接口前缀为 `/api`。

## 健康检查

```bash
curl http://localhost:8090/api/health
```

## 项目结构

参见 `docs/superpowers/plans/2026-05-09-01-foundation.md`。
```

- [x] **Step 2：创建 `wx-app/README.md`**

```markdown
# pet-visit 小程序前端

uni-app + Vue 3 + TypeScript + Pinia。

## 启动开发环境

1. 安装依赖：

```bash
pnpm install
```

2. 启动 H5 调试：

```bash
pnpm dev:h5
```

或微信小程序开发者工具调试：

```bash
pnpm dev:mp-weixin
```

## 环境变量

`.env.development` / `.env.production` 中配置 `VITE_API_BASE_URL`。

## 项目结构

参见 `docs/superpowers/plans/2026-05-09-01-foundation.md`。
```

- [x] **Step 3：提交**

```bash
git add serve/README.md wx-app/README.md
git commit -m "docs: update serve and wx-app README for P1"
```

---

## P1 完成确认

- [x] **Step 1：全部任务勾选完毕**

检查本文件所有 `- [ ]` 都改为 `- [x]`，并在 [README.md](./README.md) 中勾选 `P1-T1` ~ `P1-T20`。

- [x] **Step 2：在 Master 文档中标记 P1 状态为 ✅**

编辑 [README.md](./README.md)，将 P1 行状态从 `🟡 已编写，待实施` 改为 `✅ 已完成`。

- [x] **Step 3：打 tag**

```bash
cd /Users/timer/Document/Code/ai-code/pet-visit && git tag p1-done
```

- [x] **Step 4：准备进入 P2**

与用户确认是否进入 P2（用户体系）阶段。

---

## 完成标准（验收清单）

- [x] 后端 `pnpm start:dev` 启动成功，日志美化输出
- [x] 访问 `http://localhost:8090/api/health` 返回统一响应格式
- [x] 主动触发一个错误（例如 `curl http://localhost:8090/api/notexist`），返回统一异常格式
- [x] 前端 `pnpm dev:h5` 启动成功，3 Tab 可切换
- [x] 首页"调用健康检查"按钮能获取后端数据
- [x] Token 存取工具、Pinia 持久化、环境变量、主题变量 均已就位
- [x] 所有改动都有 commit，且能通过 `git log` 看到清晰历史
